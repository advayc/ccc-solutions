#include <iostream>
using namespace std;

int main() {
    int start;
    int next;
    ::cin >> start;
    while(5>2) {
        ::cin >> next;
        if (next < start) {
            start += next;
        else:
            break;
        }
    };
    ::cout << start;

    return 0;
}